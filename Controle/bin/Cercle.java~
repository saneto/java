package Formes;
import java.*;

public class Cercle extends FormeGeometrique{
	double rayon;
	
	public Cercle(double x, double y, double r){
		posX = x; posY = y; rayon = r;
	}

	double surface(){	return Math.PI*Math.pow(rayon, 2);	}
	double perimetre(){	return 2*rayon*Math.PI;	}
}
